{
  "name": "@omnifield/weber",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Omnifield Weber — web framework monorepo (@omnifield/*): runtime/kit/builders/boost/domain/cli",
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prepare": "husky",
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "typecheck": "nx run-many -t typecheck",
    "lint": "nx run-many -t lint",
    "affected": "nx affected -t lint,typecheck,test,build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "husky": "^9.1.7",
    "nx": "^23.0.1",
    "typescript": "^5.8.3"
  }
}
