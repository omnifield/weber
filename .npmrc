# engines.node — реальный гейт, не warning (канон toolchain-pins §node)
engine-strict=true
# Пакеты экосистемы (@omnifield/*) — ПУБЛИЧНЫЙ npm (kb:ADR-19): OIDC trusted publishing,
# provenance по умолчанию, установка без токена. GitHub Packages = legacy, снят 2026-07-28
# после проверки, что @omnifield/{biome,nx}-preset опубликованы в публичный реестр.
# Отдельная строка про реестр не нужна — публичный npm и есть дефолт.
