# engines.node — реальный гейт, не warning (канон toolchain-pins §node)
engine-strict=true
# Пакеты экосистемы (@omnifield/*) — GitHub Packages.
# Auth — user-level ~/.npmrc, НЕ здесь (workstation/README §пост-шаги).
@omnifield:registry=https://npm.pkg.github.com
